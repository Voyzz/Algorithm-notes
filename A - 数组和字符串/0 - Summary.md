```JavaScript
* ✨ unshift和shift的效率比push和pop要慢100倍
    * 可以用push在reverse
* ✨ 两个栈可以构成一个队列
    * 入从第一个栈入，出从第二个栈出
    * 先判断第二个栈是否为空，为空时再将第一个栈的元素入栈到第二个栈 
* ✨ 利用辅助栈获取栈的最小值
    * 辅助栈为空、或辅助栈顶的值没有入栈的值小时再入辅助栈
    * 辅助栈栈顶就是最小值
* ✨ 利用辅助栈获取队列的最大值
    * pop出辅助队列中比value小的，保持辅助队列是递减的
    * 队列头部就是最大值
```